# Secrets
.env
token.json
client_secret*.json

# Python
__pycache__/
*.pyc
venv/
.venv/
.pytest_cache/
.ruff_cache/

# OS
.DS_Store

# Data
data/*.db

# Session memory (agent working state, not project content)
memories/

# Sources — keep pointers/READMEs/CREDITS committed, gitignore full-content pastes
# that are not explicitly licensed for redistribution. The agent reads these locally;
# the repo records THAT we used them and WHERE to find them, not the content itself.
sources/GenerativeAICourse/lessons/*.md
sources/GenerativeAICourse_README.md
sources/agents-towards-production_README.md
sources/AI PM Knowledge Store & Deep Multi-Hop Extraction Plan.md
sources/linkedin_resources/**/*.md
sources/karpathy-llm-wiki.md
